{
  "private": true,
  "scripts": {
    "build": "pulp build --to dist/app.js",
    "watch": "pulp -w build --to dist/app.js"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.6"
  }
}
