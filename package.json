{
  "private": true,
  "scripts": {
    "build": "pulp build",
    "watch": "pulp -w build",
    "docs": "pulp build -I test --to docs/example.js"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.6"
  }
}
