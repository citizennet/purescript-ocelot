{
  "private": true,
  "scripts": {
    "build-all": "npm run build-ui && npm run build-css-dev",
    "postinstall": "bower i --silent && cd css && npm install",
    "clean": "rm -rf output bower_components node_modules && cd css && rm -rf node_modules",
    "build-ui": "pulp build -I ui-guide --to dist/example.js",
    "watch-ui": "pulp -w --then 'npm run build-css-dev' build -I ui-guide --to dist/example.js",
    "build-css-dev": "cd css && npm run build-dev",
    "build-css-prod": "cd css && npm run build-prod"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.6"
  }
}
