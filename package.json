{
  "private": true,
  "scripts": {
    "build": "pulp build",
    "example-base": "pulp build --include examples/base/src --to examples/base/dist/example.js",
    "watch-base": "pulp build --include examples/base/src --to examples/base/dist/example.js"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.6"
  }
}
